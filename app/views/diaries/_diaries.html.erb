  <%= d.vegtable %>
  <%= d.action %>
  <% if d.avatar?  %>
    <%= image_tag d.avatar.url  %>
  <% end %>
  <%= d.date %>
  <%= link_to "詳細", diary_path(d.id), method: :get %>
  <p>-------------------------</p>